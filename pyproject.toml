[project]
name = "spc-voice-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "livekit-agents[cartesia,deepgram,openai,silero,turn-detector]~=1.5",
    "livekit-plugins-noise-cancellation>=0.3.0",
    "python-dotenv>=1.2.3",
    "torch>=2.13.0",
]
